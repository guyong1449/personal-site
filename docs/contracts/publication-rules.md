# Publication Rules

## Core Policy

Public export is opt-in.

- a note is private by default
- content becomes public only when explicitly marked for publication

## Minimum Rule Set

Content is eligible for website export only if:

1. `publish: true`
2. `channels` contains `site`
3. required metadata is present
